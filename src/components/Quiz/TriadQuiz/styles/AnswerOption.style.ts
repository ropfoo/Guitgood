import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  pressable: {
    alignItems: 'center',
    borderRadius: 100,
    height: 80,
    justifyContent: 'center',
    marginBottom: 20,
    width: 80,
  },
  wrapper: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
  },
  text: {
    fontSize: 25,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
