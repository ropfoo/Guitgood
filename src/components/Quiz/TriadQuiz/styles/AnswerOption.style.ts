import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  pressable: {
    marginBottom: 20,
    borderRadius: 100,
    alignItems: 'center',
    justifyContent: 'center',
    width: 80,
    height: 80,
  },
  wrapper: {
    padding: 20,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 25,
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
